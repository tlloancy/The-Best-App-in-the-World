#ifndef CONVERT_HPP
#define CONVERT_HPP

#include <string>
#include <utility>

uint8_t squareFromAlg(const std::string& alg);
std::pair<int, int> fromUCI(const std::string& uci);

#endif // CONVERT_HPP
