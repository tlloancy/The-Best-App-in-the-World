#include "../../include/engine/Search.hpp"

float MinimaxSearch::impl_search(const Board& board, int depth) {
    return 0.0f;
}

float MCTSSearch::impl_search(const Board& board, int depth) {
    return 0.0f;
}