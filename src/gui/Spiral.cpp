#include "../../include/gui/Spiral.hpp"

void Spiral::render(const Matrix4& projection, const Matrix4& view) const {
    // Implémentation vide pour l'instant (à compléter)
    // Exemple : utiliser projection et view pour un rendu 3D
}